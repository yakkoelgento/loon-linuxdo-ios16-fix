# Loon linux.do iOS 16.2 WebKit Fix

Loon plugin for attempting to make linux.do load on iOS 16.2 Safari/WebKit by rewriting unsupported JavaScript class static blocks in intercepted JS responses.

Import the `.plugin` raw URL in Loon.
